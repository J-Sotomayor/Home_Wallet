# Firebase client configuration is public by design. Access control is enforced
# by Firebase Security Rules, App Check, IAM, and restricted API-key allowlists.
# Keep this exception limited to the generated client configuration files.
paths-ignore:
  - "lib/firebase_options.dart"
  - "android/app/google-services.json"
